use -I<path to this file> in the compile command for gcc and g++
after that you can use the files inside with include <...>
also add "${workspaceFolder}/client-controller/atmega328p/includes/**" in the c/c++ extention configuration for IntelliSense to work